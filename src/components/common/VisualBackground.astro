<div class="visual-background" aria-hidden="true">
  <div class="visual-background__field"></div>
  <div class="visual-background__grid"></div>
  <div class="visual-background__grain"></div>
</div>

<style>
  .visual-background {
    position: fixed;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
    background:
      radial-gradient(80rem 42rem at 74% -16%, color-mix(in srgb, var(--accent-strong) 12%, transparent), transparent 64%),
      linear-gradient(180deg, var(--bg) 0%, var(--bg-2) 54%, var(--surface-strong) 100%);
  }

  .visual-background__field,
  .visual-background__grid,
  .visual-background__grain {
    position: absolute;
    inset: 0;
  }

  .visual-background__field {
    opacity: 0.84;
    background:
      radial-gradient(62rem 30rem at 76% -6%, var(--ambient-strong), transparent 62%),
      radial-gradient(48rem 26rem at 6% 24%, var(--ambient-soft), transparent 66%),
      radial-gradient(38rem 24rem at 94% 78%, var(--ambient-cool), transparent 70%),
      linear-gradient(118deg, transparent 18%, var(--ambient-line) 48%, transparent 78%);
    background-size: 118% 118%, 112% 112%, 106% 106%, 148% 148%;
    animation: ambient-shift 28s var(--ease-out) infinite alternate;
  }

  .visual-background__grid {
    opacity: 0.42;
    mask-image: linear-gradient(to bottom, black 0%, black 54%, transparent 92%);
    background-image:
      linear-gradient(to right, var(--grid-line) 1px, transparent 1px),
      linear-gradient(to bottom, var(--grid-line) 1px, transparent 1px),
      linear-gradient(to right, color-mix(in srgb, var(--accent) 10%, transparent) 1px, transparent 1px),
      linear-gradient(to bottom, color-mix(in srgb, var(--accent) 10%, transparent) 1px, transparent 1px);
    background-position: center top;
    background-size: 72px 72px, 72px 72px, 288px 288px, 288px 288px;
    animation: grid-drift 38s linear infinite;
  }

  .visual-background__grain {
    opacity: 0.11;
    background-image:
      repeating-radial-gradient(circle at 17% 23%, rgba(255, 255, 255, 0.42) 0 1px, transparent 1px 5px);
    mix-blend-mode: overlay;
  }

  .visual-background::after {
    position: absolute;
    inset: 0;
    opacity: 0.5;
    background:
      linear-gradient(90deg, transparent 0 49.8%, color-mix(in srgb, var(--accent) 12%, transparent) 50%, transparent 50.2%),
      linear-gradient(180deg, transparent 0 49.8%, color-mix(in srgb, var(--accent-strong) 10%, transparent) 50%, transparent 50.2%);
    background-size: 100% 190px, 240px 100%;
    mask-image: radial-gradient(circle at 50% 18%, black, transparent 72%);
    content: "";
  }

  @keyframes ambient-shift {
    from {
      background-position: 0% 0%, 0% 0%, 100% 40%, 0% 0%;
    }

    to {
      background-position: 6% 3%, 4% 8%, 94% 48%, 18% 12%;
    }
  }

  @keyframes grid-drift {
    from {
      background-position: center top;
    }

    to {
      background-position: calc(50% + 72px) 72px;
    }
  }

  @media (max-width: 720px) {
    .visual-background__grid {
      background-size: 54px 54px;
      opacity: 0.24;
    }

    .visual-background__field {
      opacity: 0.74;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .visual-background__field,
    .visual-background__grid {
      animation: none;
    }
  }
</style>
